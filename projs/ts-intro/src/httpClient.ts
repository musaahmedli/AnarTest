// interface IHTTPRClient {
//     getReq: <T>(url: string) => Promise<T>;
// }

// class HTTPClient implements IHTTPRClient {
//   getReq = async <T>(url: string): Promise<T> => {
//     const res = await fetch(url);
//     return await res.json();
//   };
// }

// const httpClient = new HTTPClient();
// export default httpClient;
